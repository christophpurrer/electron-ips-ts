{
  "name": "electron-ts",
  "version": "1.0.0",
  "description": "Yet another Electron application using ipcRenderer.invoke()",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf build && mkdir build",
    "prestart": "yarn clean && yarn build",
    "start": "electron ./dist/electron/main.js"
  },
  "author": "Kevin Hirczy <https://nehalist.io>",
  "license": "MIT",
  "devDependencies": {
    "electron": "^9.0.0",
    "typescript": "^3.8.3"
  }
}
